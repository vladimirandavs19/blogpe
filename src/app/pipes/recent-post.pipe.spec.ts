import { RecentPostPipe } from './recent-post.pipe';

describe('RecentPostPipe', () => {
  it('create an instance', () => {
    const pipe = new RecentPostPipe();
    expect(pipe).toBeTruthy();
  });
});
